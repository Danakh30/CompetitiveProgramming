#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, g;
    cin >> n;
    int maxi = INT_MIN;
    while (n--)
    {

        for (int i = 0; i < g; i++)
        {
            cin >> g;
        }
    }
    return 0;
}
